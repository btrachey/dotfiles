if [[ ${#argv[@]} -gt 1 ]]; then
  if  [[ $1 == +* ]]; then
    linenum="$1"; shift
    "$EDITOR" "$linenum" "$1"
  else
    echo "multiple args currently unsupported"
  fi
else
  "$EDITOR" "${1:-.}"
fi
  
